
const initialState = {
  comingFromLogin: false,
  credentials: [],
};

export default function reducer(state = initialState, action) {
  switch (action.types) {
    default:
      return state;
  }
}
